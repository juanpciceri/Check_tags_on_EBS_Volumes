# Check_tags_on_EBS_Volumes
This python lambda function will give return a list with all the tags for all the volumes in your accout, also could evaluate if an specific tag is set in your EBS volumes
